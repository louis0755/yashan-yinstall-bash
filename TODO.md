# 需求清单

## 已确认

- `yinstall` 与 `myas` 保持独立版本和维护。
- 接收完整软件包路径，不依赖软件包命名规则。
- 支持 `myas` 传入明确的集群、目录和数据库端口参数。
- 支持不使用 SSH 传输的本地单机调用。
- 接收 `myas` 传入的 sys 密码并用于数据库部署。

## 后续

- 使用生成的 YashanDB 配置模板执行虚拟机集成测试。
- 按具体版本审核后扩展主备拓扑操作文档。
